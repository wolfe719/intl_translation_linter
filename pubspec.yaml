name: intl_translation_linter
description: Intl Translation Linter is a Flutter custom linting plugin that helps catch missing translation strings in your code.
version: 1.0.2
homepage: https://github.com/wolfe719
repository: https://github.com/wolfe719/intl_translation_linter
environment:
  sdk: ^3.4.2

# Add regular dependencies here.
dependencies:
  analyzer: ^6.2.0
  analyzer_plugin: ^0.11.3
  custom_lint_builder: ^0.6.4

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.8

# Include your custom lint as a dependency (replace with the actual package name)
# dependency_overrides:
#    smarter_translate_lints: ^1.0.0
